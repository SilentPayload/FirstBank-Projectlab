# Copy this file to .env and fill in your values.
# Each student receives a unique STUDENT_SEED from their instructor.

# Your assigned seed (integer).  Same seed = reproducible dataset.
STUDENT_SEED=12345

# Splunk admin password.  Must satisfy Splunk complexity rules
# (8+ chars, mixed case, digit, special char).
SPLUNK_PASSWORD=Training123!
